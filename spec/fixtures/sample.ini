; Assertions live in the comments: `<- scope` checks the marker's own column
; on the previous non-comment line, `^ scope` checks the caret's. Scopes
; match by prefix, so the trailing `.ini` segment is left off.

[server]
; <- punctuation.definition.section.begin.bracket.square
;^ entity.name.section
;      ^ punctuation.definition.section.end.bracket.square

host = example.com
; <- variable.other.key
;    ^ punctuation.separator.key-value
;      ^ string.unquoted

port = 8080
;      ^ constant.numeric

debug = true
;       ^ constant.language.boolean

# a hash comment
; <- punctuation.definition.comment
